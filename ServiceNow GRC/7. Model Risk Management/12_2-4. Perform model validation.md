### Perform model validation

| Field | Description |
| --- | --- |
| Validation checklist template | Predefined template that outlines the structure, questions, and scoring logic for the assessment. This field is auto-populated with the default assessment template. |
| Validation assignee | Individual assigned to complete the validation task. This field is auto-populated with the assignee name selected as a validator in the stakeholder list. You can select only those users who are specified as stakeholders. |
| Due date | Date by which the validation task must be completed. |

6.Select Submit.
After you submit your responses, you are no longer able to modify them.
Result
The model risk task is submitted for approval. If no approvers are configured, the model directly
moves to the Pre-deployment state.
Create a validation task
Create a validation task by selecting a predefined validation template, assigning a validator, and
setting a due date. This task defines the validation scope and responsibilities, preparing the
model for formal validation and deployment.
Before you begin
Role required: sn_model_risk_mgmt.model_governance or
sn_model_risk_mgmt.risk_coordinator
About this task
You can create a validation task for a model when it is in the Pre-deployment or Monitor stage.
Procedure
1.Navigate to All > Model Risk Workspace.
2. Select the list icon .
3. Select the model for which you want to create a validation task.
4.In the Pre-deployment state, select Create task.
5. On the Model Validation dialog, fill in the fields.
Model Validation
Field Description
Validation Predefined template that outlines the structure, questions, and scoring logic
checklist for the assessment. This field is auto-populated with the default assessment
template template.
Validation Individual assigned to complete the validation task. This field is auto-populated
assignee with the assignee name selected as a validator in the stakeholder list. You can
select only those users who are specified as stakeholders.
Due date Date by which the validation task must be completed.
What to do next
Perform model validation
Perform model validation
Perform model validation to assess and validate the model, collect required documents, report
any issues, and determine if the model is fit for use. This process confirms that the model meets
required standards and is ready for deployment.
Before you begin
Role required: sn_model_risk_mgmt.model_validator
About this task
A model risk validation task contains the following stages:

•Document collection
•Validation checklist
•Issues
•Validation outcome
You can perform the listed activities in any sequence.
Procedure
1.To perform model validation from the GRC tasks page, navigate to All > Self-Service >
Employee Center and do the following:
a.Under My active items, select GRC tasks.
b.Select the model validation from the My to-dos card.
2. To perform model validation from the Model Risk Workspace, navigate to All > Model Risk
Workspace and do the following:
a.Select the list icon .
b.From the list, navigate to Models > Pre-deployment.
c.Select the model for which you want to perform validation.
3. In the Pre-deployment state, select Open task.
The model risk validation task workflow opens.
4.To send an evidence collection request, select Create Request.
For more information, see Request evidence for model.
5. In the Validation Checklist stage, select Open to perform the validation.
6.Respond to all the factors or questions listed in the validation task.
7.Select Submit.
After you submit your responses, you’re no longer able to modify them.
8.Optional: In the Issues stage, add new or existing issues with the model.
When you add new or existing issues, they are associated with the model risk and the model
risk task.

| Field | Description |
| --- | --- |
| Name | Unique name of the evidence request. |
| Due date | Expected date of evidence submission. |
| Assignment group | Group assigned to provide evidence. |
| Assigned to | User responsible for providing the evidence. |
| Evidence collection instructions | Instructions for providing evidence. For example, list of supporting documents, files, and so on. |

9.In the Validation Outcome stage, select the outcome whether the model is fit to use or not and
send it for approval.
Note: All activities must be completed before sending the task for approval.
Result
The model validation task is submitted for approval. If no approvers are configured, the task is
marked as completed.
Related topics
Request evidence for model
Request evidence for model
Send evidence collection request for all the required documents to complete the model
validation. Evidence request helps validators to electronically request the information that they
need from the model owner to perform model validation.
Before you begin
Role required: sn_model_risk_mgmt.model_validator
Procedure
1.Navigate to All > Model Risk Workspace.
2. Select the list icon .
3. From the list, navigate to Models > Pre-deployment.
4.Select the model for which you want to request evidence.
5. To send an evidence collection request, select Create Request.
6.On the form, fill in the fields.
For a description of the field values on the Evidence request form, see Request evidence form.
7.Select Request.
Request evidence form
Use this form to electronically request the information needed from the model owner to perform
model validation.
See the following table for a description of the field values.
Document request form
Field Description
Name Unique name of the evidence request.
Due date Expected date of evidence submission.
Assignment group Group assigned to provide evidence.
Assigned to User responsible for providing the evidence.
Evidence collection instructions Instructions for providing evidence. For
example, list of supporting documents, files,
and so on.

> **[Screenshot: Model Risk Workspace — Perform model validation workflow]**
> A ServiceNow platform screenshot showing a model risk validation task record (MRT0001049) for "VaR-Cashflow Forecasting Model". The breadcrumb reads "Tasks > Model risk task: MRT0001049". Header: Record type = Model risk task, Task type = Validation, State = In-Progress, Assignee = Abraham Lincoln, Model risk = VaR-Cashflow Forecasting Model. Tabs: Workflow, Details.
> 
> The left "Task Workflow" panel shows: In Progress (orange dot, active, 0/4 progress bar). Sub-stages listed: Document Collection (red dot, active), Validation Checklist (grey circle), Issues (grey circle), Validation Outcome (grey circle). Below: Approval stage (grey, Pending, with expand icon).
> 
> The right panel shows: "● In Progress — Document Collection". Description: "Send evidence collection request for all the documents needed to complete validation." A search icon with text "No evidence request created / Create and assign task". A blue "Create Request" button. A grey "Skip" button in the bottom-right corner.