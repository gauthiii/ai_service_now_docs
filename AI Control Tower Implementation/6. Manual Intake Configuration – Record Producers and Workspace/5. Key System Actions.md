# Key System Actions

> ServiceNow AI Control Tower (AICT) — Implementation Guide
> Section: Manual Intake Configuration – Record Producers and Workspace → Key System Actions
> Source: AI-Control-Tower.pdf, p.53

---

## What Is This?

This subtopic describes the key system actions triggered by the "Request an AI Use Case" record producer. This intake form captures AI use case details and creates the foundational AI System Digital Asset. It explains the records this record producer creates and what each created record configures and stores within the manual intake flow.

---

## Record Producer: Request an AI Use Case

This intake form captures AI use case details and creates the foundational AI System Digital Asset.

### Key System Actions

- **Creates** AI System Digital Asset

  ```
  [alm_ai_system_digital_asset]
  ```

  - Sets class, state, ownership, and model category
  - Stores associated models and datasets

- **Creates** AI System Component Product Model

  ```
  [cmdb_ai_system_component_product_model]
  ```

---

## How to Use This in Implementation

When an implementer configures the "Request an AI Use Case" record producer, they should ensure it performs these system actions: creating the AI System Digital Asset (`alm_ai_system_digital_asset`) record, which establishes class, state, ownership, and model category and stores associated models and datasets, and creating the AI System Component Product Model (`cmdb_ai_system_component_product_model`) record. These created records form the foundational AI System asset that downstream lifecycle and governance workflows depend on.
