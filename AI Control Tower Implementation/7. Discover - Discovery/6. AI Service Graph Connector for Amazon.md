# AI Service Graph Connector for Amazon

> ServiceNow AI Control Tower (AICT) — Implementation Guide
> Section: Discover - Discovery → AI Service Graph Connector for Amazon
> Source: AI-Control-Tower.pdf, p.67

---

## What Is This?

The AI Service Graph Connector for Amazon is used to discover AI assets and usage data from AWS services into ServiceNow AI Control Tower. Supported assets may include AI systems, agents, models, tools, prompts, and usage records. It is part of the Discover/Discovery capability that feeds visibility into AI activity, adoption, and value realization.

---

## Purpose

The AI Service Graph Connector for Amazon is used to discover AI assets and usage data from AWS services into ServiceNow AI Control Tower. Supported assets may include AI systems, agents, models, tools, prompts, and usage records.

---

## Required Roles

- `sn_ai_disc.discovery_admin`
- `sn_cmdb_int_util.sgc_admin`

---

## AWS Prerequisites

Before creating an Amazon AI connection, confirm that your organization has:

- An active AWS account.
- IAM credentials with the required read permissions.
- API access enabled for the AWS services being connected, including Amazon Bedrock, Amazon SageMaker, Amazon CloudWatch, and Amazon Bedrock AgentCore, as applicable.

---

## Required IAM Permissions

| AWS Service | Required Permissions |
| --- | --- |
| Amazon Bedrock | `bedrock:List*`, `bedrock:Get*` |
| Amazon SageMaker | `sagemaker:List*`, `sagemaker:Describe*` |
| Amazon CloudWatch | `logs:DescribeLogGroups`, `logs:DescribeLogStreams`, `cloudwatch:GetMetricData` |
| Amazon Bedrock AgentCore | `bedrock:ListAgents`, `bedrock:GetAgent` |

---

## Supported AWS Services

The Amazon connector supports discovery and usage data from:

- Amazon Bedrock
- Amazon Bedrock AgentCore
- Amazon SageMaker
- Amazon CloudWatch Logs

---

## Connection Outcome

(Heading appears at the bottom of this page; its body content continues on the following page and is not reproduced here.)

---

## How to Use This in Implementation

To implement this connector, first ensure the executing user holds the `sn_ai_disc.discovery_admin` and `sn_cmdb_int_util.sgc_admin` roles. Confirm the AWS prerequisites are met: an active AWS account, IAM credentials with read permissions, and API access enabled for each AWS service in scope (Amazon Bedrock, Amazon SageMaker, Amazon CloudWatch, and Amazon Bedrock AgentCore). Grant the IAM permissions listed per service so the connector can list and describe AI assets and pull usage data. Once connected, discovered AI assets and usage records from the supported AWS services feed into AI Control Tower, where the value dashboard consumes the usage data to support visibility into AI activity, adoption, and value realization.
