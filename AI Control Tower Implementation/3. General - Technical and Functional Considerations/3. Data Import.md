# Data Import

> ServiceNow AI Control Tower (AICT) — Implementation Guide
> Section: General - Technical and Functional Considerations → Data Import
> Source: AI-Control-Tower.pdf, p.18

---

## What Is This?

Data Import covers how to populate the AI Control Tower with your organization's AI inventory. This is typically the first implementation task after plugin activation. It defines the primary patterns for importing AI asset data and the key fields that should be mapped to establish lifecycle accountability and governance coverage from the start.

---

## Overview

Populating the AI Control Tower with your organization's AI inventory is typically the first implementation task after plugin activation. There are three primary patterns for importing AI asset data, and most implementations use a combination of all three. The appropriate pattern depends on whether your organization needs real-time synchronization, bulk batch loading, or native platform integration.

---

## Import Patterns

| Pattern | Best For | Considerations |
|---------|----------|----------------|
| API Integration | Organizations maintaining a live AI registry in another system requiring real-time synchronization. | Use Scripted REST APIs for complex payload structures. Requires development effort for initial setup. |
| Data Source Imports | Bulk one-time or scheduled batch loads from spreadsheets or external databases using import sets with transform maps. | Works well for organizations that do not require real-time sync. Requires field mapping configuration. |
| Native Data Sources | Organizations already on the Now Platform are seeking the most seamless path with no manual mapping required. | Record Producers and the Now Assist Sync job offer the lowest-friction path. MCP server assets can be synced automatically. |

---

## Key Field Mappings

When importing AI assets, organizations should map source data to out-of-the-box (OOB) fields to ensure consistency. These fields are critical to establish lifecycle accountability and governance coverage from day one.

> Note: On page 18, the Key Field Mappings table itself begins on the following page and is not reproduced here.

---

## How to Use This in Implementation

After activating the required plugins, plan your AI inventory import as the first implementation task. Choose among the three patterns based on your needs: use API Integration (Scripted REST APIs) when you require real-time synchronization with an external live AI registry; use Data Source Imports (import sets with transform maps) for bulk one-time or scheduled batch loads from spreadsheets or external databases; and use Native Data Sources (Record Producers, the Now Assist Sync job, and automatic MCP server asset sync) when you are already on the Now Platform and want the lowest-friction path with no manual mapping. Most implementations combine all three. When importing, map your source data to the out-of-the-box (OOB) fields to ensure consistency and to establish lifecycle accountability and governance coverage from day one.
