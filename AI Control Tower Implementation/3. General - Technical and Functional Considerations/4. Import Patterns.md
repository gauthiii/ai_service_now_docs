# Import Patterns

> ServiceNow AI Control Tower (AICT) — Implementation Guide
> Section: General - Technical and Functional Considerations → Import Patterns
> Source: AI-Control-Tower.pdf, p.18

---

## What Is This?

Import Patterns describe the primary approaches available for populating the AI Control Tower with an organization's AI asset data. Populating AICT with the AI inventory is typically the first implementation task after plugin activation. There are three primary patterns, and most implementations use a combination of all three; the appropriate pattern depends on whether the organization needs real-time synchronization, bulk batch loading, or native platform integration.

---

## Import Patterns

| Pattern | Best For | Considerations |
|---------|----------|----------------|
| API Integration | Organizations maintaining a live AI registry in another system requiring real-time synchronization. | Use Scripted REST APIs for complex payload structures. Requires development effort for initial setup. |
| Data Source Imports | Bulk one-time or scheduled batch loads from spreadsheets or external databases using import sets with transform maps. | Works well for organizations that do not require real-time sync. Requires field mapping configuration. |
| Native Data Sources | Organizations already on the Now Platform are seeking the most seamless path with no manual mapping required. | Record Producers and the Now Assist Sync job offer the lowest-friction path. MCP server assets can be synced automatically. |

---

## How to Use This in Implementation

When populating the AI Control Tower, choose the import pattern (or combination of patterns) based on your organization's synchronization and data-source needs:

- Use **API Integration** with Scripted REST APIs when an external live AI registry must stay in real-time sync; plan for development effort during initial setup.
- Use **Data Source Imports** (import sets with transform maps) for one-time or scheduled bulk loads from spreadsheets or external databases; configure field mappings as part of setup.
- Use **Native Data Sources** (Record Producers and the Now Assist Sync job) when already on the Now Platform for the lowest-friction path with no manual mapping; MCP server assets can be synced automatically.
