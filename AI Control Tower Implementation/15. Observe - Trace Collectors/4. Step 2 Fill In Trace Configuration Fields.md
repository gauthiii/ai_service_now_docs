# Step 2 Fill In Trace Configuration Fields

> ServiceNow AI Control Tower (AICT) — Implementation Guide
> Section: Observe - Trace Collectors → Step 2 Fill In Trace Configuration Fields
> Source: AI-Control-Tower.pdf, p.197

---

## What Is This?

This is the second step in the procedure for configuring a trace collector in AI Control Tower. After accessing the trace configuration and selecting a cloud provider connector, the implementer completes all required fields in the trace configuration form. This step defines the naming, credentials, polling cadence, MID Server, and provider-specific settings that govern how trace data is collected from the cloud provider.

---

## Trace Configuration Fields

Complete all required fields in the trace configuration form. The table below describes each field, its purpose, and guidance for implementation teams.

| Field | Req. | Description | Implementation Guidance |
|-------|------|-------------|--------------------------|
| Name | ✓ | A descriptive label for this trace configuration. | Use a naming convention that identifies the provider, environment, and purpose. Example: AWS-Prod-AgentCore-1 |
| Credential | ✓ | Authentication credentials for your cloud provider. | Pre-create in the Credentials module. Test connectivity before saving the trace config. Feb release: AWS and GCP only. Mar release: Azure added. |
| Collection Frequency (minutes) | ✓ | Polling interval (in minutes) for the MID Server to collect new trace data from the cloud provider. | Align with AI agent owners on frequency. Higher frequency increases data freshness but adds load. Start with 15–30 minutes and adjust. |
| MID Server | ✓ | The ServiceNow MID Server that executes the trace collection process. | Verify MID Server status is Active and validated before selecting. Reference: MID Server Installation Docs. |
| Credential Alias | * | API Key: Credential alias for routing trace data to the ServiceNow observability service. | Feb Innovation Lab release only. Create the credential alias in advance. Not required for Mar release. |
| AWS Region | * | AWS region where AI agents are deployed. | AWS AgentCore connections only. Confirm with your cloud team. Using the wrong region will result in no traces collected. |

Note: * Required only for specific releases or providers as noted.

---

## How to Use This in Implementation

Apply this step after completing Step 1 (Access Trace Configuration) and selecting the cloud provider connector. Before filling in the form, pre-create the cloud provider credentials in the Credentials module and test connectivity, and ensure the target MID Server is Active and validated. Set the Collection Frequency to balance data freshness against system load (start with 15–30 minutes). Provide the marked-optional fields only where applicable: Credential Alias for the Feb Innovation Lab release, and AWS Region for AWS AgentCore connections (an incorrect region results in no traces being collected). Note release-specific provider support: Feb release supports AWS and GCP, with Azure added in the Mar release. After filling in the fields, proceed to Step 3 (Submit and Verify).
