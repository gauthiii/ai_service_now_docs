# Approval Request and Lifecycle Execution

> ServiceNow AI Control Tower (AICT) — Implementation Guide
> Section: Govern - Lifecycle → Approval Request and Lifecycle Execution
> Source: AI-Control-Tower.pdf, p.175

---

## What Is This?

This subtopic describes the execution layer of AI Control Tower lifecycle governance. While the Governance Details record defines the lifecycle, the AI Asset Approval Request operationalizes it and drives progression across all lifecycle phases. Lifecycle Tasks created within the Approval Request carry out the discrete governance and compliance activities, providing structured tracking and accountability across AI asset types.

---

## Approval Request and Lifecycle Execution

The AI Asset Approval Request `[sn_ai_governance_assessment_request]` is the execution layer that operationalizes the lifecycle defined in the Governance Details record.

- Each Approval Request references the AI Asset Governance Details record and acts as the container for lifecycle execution activities
- Approval Requests are configured with `Type = "AI Governance Lifecycle"` to scope them explicitly for AI Control Tower lifecycle orchestration
- The Approval Request drives progression across all lifecycle phases

---

## Lifecycle Task Framework

AI Asset Approval Tasks `[sn_ai_governance_assessment_task]` are created within the Approval Request to drive lifecycle execution. These tasks:

- Represent discrete governance and compliance activities across the Assess, Build & Test, and Deploy phases
- Enable structured tracking and accountability
- Apply across all AI asset types: AI Systems, AI Models, Datasets, Prompts, and Inputs and Outputs

---

## How to Use This in Implementation

When implementing lifecycle governance in AICT, create an AI Asset Approval Request for each Governance Details record that requires lifecycle orchestration, setting its `Type` field to `"AI Governance Lifecycle"`. Use the Approval Request as the container that drives progression across lifecycle phases. Within each Approval Request, create AI Asset Approval Tasks to represent the discrete governance and compliance activities of the Assess, Build & Test, and Deploy phases. Because tasks apply across all AI asset types (AI Systems, AI Models, Datasets, Prompts, and Inputs and Outputs), this framework provides consistent, structured tracking and accountability throughout the lifecycle.
