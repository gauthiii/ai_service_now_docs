# Intake Form Configuration – AICT Workspace

> ServiceNow AI Control Tower (AICT) — Implementation Guide
> Section: Summary of AI System asset - State Transitions & Tasks → Intake Form Configuration – AICT Workspace
> Source: AI-Control-Tower.pdf, p.191

---

## What Is This?

This subtopic is part of the AICT Customization Guide. It explains how customers who add custom fields to the AI asset tables or product model tables can surface those custom fields on the AICT workspace's asset intake form. It fits into implementation as a configuration step for tailoring the intake experience to organization-specific data captured for different types of AI Assets.

---

## Adding Custom Fields to the AICT Workspace Intake Form

If customers choose to add custom fields to the AI asset tables or product model tables for various types of AI Assets, they can make these fields available on the AICT workspace's asset intake form by following the steps below:

1. As an admin user, open the form view for the table containing your custom fields in UI16.
2. Change the form view to "Intake". The new form view should resemble what you see in the AICT intake form.
3. Right click on the form header and open form builder to update the "Intake" view.
4. Add your custom field to the form layout and save it.
5. Open the intake form in AICT and your custom field should now be visible on the page.

Intake Form Configuration – Employee Centre (MS1)

---

## How to Use This in Implementation

An implementer applies this when an organization needs to capture additional, custom data on the AICT intake form beyond the out-of-the-box fields. Prerequisites: admin access and custom fields already defined on the relevant AI asset tables or product model tables. The configuration is done through the standard UI16 form view tooling — switching to the "Intake" view and using the form builder to add the custom field to the layout. After saving, the field appears on the AICT intake form. Note that the "Intake" form view is the specific view that drives what the AICT workspace renders, so edits must be made to that view rather than the default form view.
